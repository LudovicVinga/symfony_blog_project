<!DOCTYPE html>
<html lang="fr">
<head>

    {# Encodage standard des caractères pour le web #}
    <meta charset="UTF-8">

    {# Minimum de Responsive Design à toujours mettre en place #}
    <meta name="viewport" content="width=device-width, initial-scale=1.0">

    {# La description de la page #}
    <meta name="description" content="{% block description %}{% endblock %}">

    {# Le titre de la page #}
    <title> {% block title %}{% endblock %} </title>

    {# SEO - Autorisation aux robots des moteurs de recherche d'indexer et de suivre la page#}
    <meta name="robots" content="noindex, nofollow">

    {# Google font #}
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Pacifico&family=Poppins:ital,wght@0,100;0,200;0,300;0,400;0,500;0,600;0,700;0,800;0,900;1,100;1,200;1,300;1,400;1,500;1,600;1,700;1,800;1,900&display=swap" rel="stylesheet">

    {# Bootstrap v5.3 CSS #}
    {% block stylesheets %}
        {{ encore_entry_link_tags('app') }}
    {% endblock %}

    {# Bootstrap v5.3 js #}
    {% block javascripts %}
        {{ encore_entry_script_tags('app') }}
    {% endblock %}

</head>
<body class="bg-light">

    {# Navbar #}
    <nav class="navbar navbar-expand-lg bg-secondary shadow mb-5 py-3">
        <div class="container-fluid">
            <a class="navbar-brand" href="{{ path('app_visitor_welcome') }}">BLOG PROJECT</a>
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navbarNav">
            <ul class="navbar-nav ms-auto">
                <li class="nav-item">
                    <a class="nav-link active" aria-current="page" href="{{ path('app_user_home') }}">Espace utilisateur</a>
                </li>
                
                {% if is_granted('ROLE_ADMIN') %}
                    <a class="nav-link" href="{{ path('app_admin_home') }}">Espace Admin</a>
                {% endif %}
                
                <li class="nav-item">
                    <a class="nav-link" href="#">Mon profil</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('app_visitor_welcome') }}">Retour au site</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('app_logout') }}">Déconnexion</a>
                </li>
            </ul>

            </div>
        </div>
    </nav>

    {# Main #}
    <main>
        {% block main %}{% endblock %}
    </main>

    {# Footer #}
    <footer class="text-center">
        <span>Dwwm20</span>
    </footer>

</body>
</html>
